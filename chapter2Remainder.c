#include<stdio.h>
int main (){
    // int a=5, b=10;
    // printf("the value of a is %d and value of b is %d " ,a,b);
    
    // Modulous is used to find the remainder 
    int a = 2, b = 9;
    printf("The remainder is %d when b is divided by a",b%a);
    //a^b is not used for exponentation in c language
    // arithmetic operator between int and int is always a int 
    return 0;
} 